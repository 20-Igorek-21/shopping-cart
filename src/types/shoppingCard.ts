export interface IShoppingProduct {    id: string;    title: string;    image: string;    cost: IShoppingProductCost;    deleted?: boolean,}export interface IShoppingProductCost {    promotional_product: boolean    price: number;    discount: number;    quantity: number;    totalAmount: number;    totalAmountWithDiscount: number;    discountAmount: number;}export type IProductOrderType = Pick<IShoppingProduct, 'id' | 'title'> & Pick<IShoppingProductCost, 'quantity' | 'totalAmount'>