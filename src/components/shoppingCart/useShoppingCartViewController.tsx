import {IProductOrderType} from "../../types";import useShoppingCartViewModel from "./useShoppingCartViewModel";const useShoppingCartViewController = () => {    const {products, totalPrice, productDeleted} = useShoppingCartViewModel()    const onPlaceOrder = () => {        const orderProducts: IProductOrderType[] = products.map((product) => {            const {promotional_product, totalAmountWithDiscount, totalAmount} = product.cost;            return {                id: product.id,                title: product.title,                quantity: product.cost.quantity,                totalAmount: promotional_product ? totalAmountWithDiscount : totalAmount,            }        })        console.log(orderProducts)    }    return {        products,        totalPrice,        onPlaceOrder,        productDeleted    }};export default useShoppingCartViewController;