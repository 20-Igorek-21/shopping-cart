import useShoppingProductListViewModel from "./useShoppingProductListViewModel";const useShoppingProductListViewController = () => {    const {products, updateQuantity, deleteCard, restoreCard} = useShoppingProductListViewModel();    return {        products,        updateQuantity,        deleteCard,        restoreCard,    }};export default useShoppingProductListViewController;