import React from 'react';import ShoppingProductCardView from "../shoppingProductCard/ShoppingProductCardView";import {observer} from "mobx-react-lite";import useShoppingProductListViewController from "./useShoppingProductListViewController";import {IShoppingProduct} from "../../types";const ShoppingProductListView = observer(() => {    const {        products,        updateQuantity,        deleteCard,        restoreCard,    } = useShoppingProductListViewController()    return (        <>            {products.map((product: IShoppingProduct) => (                <ShoppingProductCardView                    updateQuantity={updateQuantity}                    deleteCard={deleteCard}                    restoreCard={restoreCard}                    product={product}                    key={product.id}                />            ))}        </>    );});export default ShoppingProductListView;